// jest.config.js
module.exports = {
  testEnvironment: 'node', // Indique à Jest d'utiliser un environnement Node.js (pour le backend)
  // Ajoutez d'autres configurations si vous en avez (par exemple, pour React comme vu précédemment)
  // preset: '@shelf/jest-mongodb', // Alternative si vous utilisez @shelf/jest-mongodb
};